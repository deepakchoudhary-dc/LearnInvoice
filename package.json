{
  "name": "ledgerly-offline-invoice-vault",
  "version": "0.1.0",
  "private": true,
  "description": "Free, offline-first, privacy-first invoicing and billing for small businesses. Encrypted local vault, GST-ready invoices, POS, reports — no cloud, no accounts.",
  "keywords": ["invoice", "invoicing", "billing", "pos", "gst", "offline-first", "privacy", "small-business"],
  "license": "MIT",
  "type": "module",
  "scripts": {
    "start": "tsx src/server.ts",
    "build": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "test": "node src/tests/math.test.js && node src/tests/gst.test.js && node src/tests/documents.test.js && node src/tests/csv.test.js && node src/tests/backup.test.js && node src/tests/complete_business.test.js"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "22.7.6",
    "ts-node": "10.9.2",
    "tsx": "4.19.2",
    "typescript": "5.6.3"
  },
  "engines": { "node": ">=18" },
  "dependencies": { "express": "^5.2.1" }
}
